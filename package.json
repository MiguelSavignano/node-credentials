{
  "name": "node-credentials",
  "version": "1.5.0",
  "description": "Encrypted json for manage all credentials, similar to rails encrypted secrets",
  "main": "src/index.js",
  "bin": "bin/node-credentials.js",
  "scripts": {
    "debug": "node --inspect-brk test/console.js",
    "test": "jest  --reporters=default --reporters=jest-junit --collect-coverage"
  },
  "repository": "https://github.com/MiguelSavignano/node-credentials",
  "keywords": [
    "secrets",
    "encrypted",
    "env"
  ],
  "author": "migue.masx@gmail.com",
  "license": "MIT",
  "dependencies": {
    "command-line-args": "^5.0.2",
    "command-line-usage": "^5.0.5",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.9",
    "yaml": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "jest-junit": "^11.0.1"
  }
}
